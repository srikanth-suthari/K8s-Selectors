apiVersion: apps/v1
kind: Deployment
metadata:
  name: taints-tolerations
spec:
  replicas: 3
  selector:
    matchLabels:
      env: dev
  template:
    metadata:
      name: taints-n-tolerations
      labels:
        env: dev
    spec:
      containers:
        - name: nginx
          image: nginx
      tolerations:
        - key: env
          operator: "Exists"
          effect: "NoSchedule"