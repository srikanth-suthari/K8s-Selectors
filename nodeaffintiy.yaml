apiVersion: v1
kind: Pod
metadata:
  name: node-affintiy
spec:
  containers:
    - name: nginx
      image: nginx
  affinity:
    nodeAffinity:
      requiredDuringSchedulingIgnoredDuringExecution:
        nodeSelectorTerms:
          - key: env
            operator: In
            values:
              - dev